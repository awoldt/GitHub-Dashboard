This web app allows you to search any public GitHub account and view repository data such as most recent pushes along with commit data.

```backend``` is an Express.js server that serves the React application. All server-side logic is coded in TypeScript then compiled into a final build folder.

```frontend``` contains all the React code